# Timer Web App

## 프로젝트 개요
개인 커스텀 타이머 웹앱. 정적 페이지로 배포하며, 모바일(iOS 우선) 환경에 최적화.

## 기술 스택
- **프론트엔드**: Vanilla HTML/CSS/JS (빌드 도구 없음)
- **스토리지**: LocalStorage (설정, 타이머 프리셋 저장)
- **배포**: 정적 호스팅 (GitHub Pages, Vercel 등)

## 타겟 환경
- **주 타겟**: iOS Safari (모바일)
- **지원**: Android Chrome, 데스크톱 브라우저
- 선택이 필요한 경우 iOS를 우선

## 프로젝트 구조
```
/
├── index.html          # 메인 페이지
├── css/
│   └── style.css       # 스타일시트
├── js/
│   ├── app.js          # 앱 진입점
│   ├── timer.js        # 타이머 로직
│   └── storage.js      # LocalStorage 관리
├── manifest.json       # PWA 매니페스트
└── icons/              # 앱 아이콘
```

## 개발 가이드라인

### LocalStorage 사용
- 키 prefix: `timer_`
- 저장 항목:
  - `timer_presets`: 사용자 정의 타이머 프리셋
  - `timer_settings`: 앱 설정 (알림음, 테마 등)
  - `timer_history`: 최근 사용 기록 (선택적)

### iOS 최적화
- `viewport` 메타태그 설정 (확대/축소 방지)
- `-webkit-tap-highlight-color` 제거
- `touch-action: manipulation` 적용
- Safe Area 대응 (`env(safe-area-inset-*)`)
- 화면 꺼짐 방지: Wake Lock API 또는 NoSleep.js
- 홈 화면 추가 시 standalone 모드 지원

### 디자인 원칙
- **현대적이고 깔끔한 UI**: 미니멀한 디자인, 충분한 여백, 명확한 시각적 계층
- **일관된 디자인 시스템**: 색상, 타이포그래피, 간격의 일관성 유지
- **부드러운 인터랙션**: 자연스러운 트랜지션과 애니메이션
- **다크/라이트 모드 지원** (`prefers-color-scheme`)

### UI/UX 원칙
- 큰 터치 타겟 (최소 44x44pt)
- 단순하고 직관적인 인터페이스
- 햅틱 피드백 고려 (가능한 경우)
- **유저 친화적 메시지**: 기술 용어 배제, 친근하고 명확한 안내 문구
- **피드백 명확화**: 사용자 액션에 대한 즉각적이고 이해하기 쉬운 반응
- **에러 메시지**: 문제 상황을 쉽게 설명하고 해결 방법 제시

### 클린코드 원칙
- **단일 책임**: 각 함수/모듈은 하나의 역할만 담당
- **의미 있는 네이밍**: 변수, 함수, 파일명은 역할을 명확히 표현
- **작은 함수**: 함수는 짧고 한 가지 일만 수행
- **DRY (Don't Repeat Yourself)**: 중복 코드 제거, 재사용 가능한 유틸리티 분리
- **일관된 코드 스타일**: 들여쓰기, 네이밍 컨벤션 통일
- **관심사 분리**: UI, 로직, 데이터 레이어 명확히 구분

### 코드 스타일
- ES6+ 문법 사용
- 모듈 패턴으로 코드 분리
- 자명한 코드 작성 (불필요한 주석 지양)
- CSS 변수로 테마 관리
- 파일당 200줄 이하 유지 권장

## 핵심 기능
1. 커스텀 타이머 생성/편집/삭제
2. 프리셋 저장 및 불러오기
3. 알림음/진동 설정
4. 백그라운드 타이머 (가능한 범위 내)

## 제약사항
- 서버 없음 (모든 데이터는 클라이언트에 저장)
- 외부 라이브러리 최소화
- 오프라인 동작 필수
